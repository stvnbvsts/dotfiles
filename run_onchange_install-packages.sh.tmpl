#!/bin/bash
# Brewfile hash: {{ include "Brewfile" | sha256sum }}
set -euo pipefail

if ! command -v brew >/dev/null 2>&1; then
  echo "Homebrew not found, skipping package install. See https://brew.sh" >&2
  exit 0
fi

brew bundle --file="{{ .chezmoi.sourceDir }}/Brewfile"
